# Assets arquivados

Imagens **não** usadas pelo site atual (nenhuma referência em HTML/CSS).

- `qrcode_*.png` — QR codes institucionais; mover para `assets/` ou referenciar se forem usados numa página.

O logotipo ativo do site é `../logotiporodas.png`.
